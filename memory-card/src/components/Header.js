import React from "react";

export default function Header(props) {
    return (
        <header>
            <div className="title">
                <h1>Memory Game</h1>
            </div>
        </header>
    );
};